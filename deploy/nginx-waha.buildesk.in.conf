# Host nginx for WAHA Plus. SSL is added by certbot --nginx -d waha.buildesk.in
# Do not put Caddy on :80/:443 — this machine already uses nginx for certificates.
#
#   sudo cp deploy/nginx-waha.buildesk.in.conf /etc/nginx/sites-available/waha.buildesk.in
#   sudo ln -sf /etc/nginx/sites-available/waha.buildesk.in /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo certbot --nginx -d waha.buildesk.in

server {
    listen 80;
    listen [::]:80;
    server_name waha.buildesk.in;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 600s;
    }
}
